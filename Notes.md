# Notes

## Let

Consists of two parts, the identifier and the expression:

```let
let <identifier> = <expression>
```

Expressions produce values, statements don't

```monkey
let x = 5 # does not produce value
5 # produces value (expression)
```

